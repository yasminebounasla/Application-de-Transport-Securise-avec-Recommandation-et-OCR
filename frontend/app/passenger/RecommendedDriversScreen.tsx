import React, { useState, } from "react";
import {
  View,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  Text,
  ActivityIndicator,
  Keyboard,
} from "react-native";

export default function RecommendationDriverScreen() {
  return (
    <>
      {/* UI */}
    </>
  );
}
