{
    // Trim extra whitespace when saving
    "files.trimTrailingWhitespace": true,

    // Ensure every file ends with a newline
    "files.insertFinalNewline": true,

    // Format code automatically when saving
    "editor.formatOnSave": true
    
}