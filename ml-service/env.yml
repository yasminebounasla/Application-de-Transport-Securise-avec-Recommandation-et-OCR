name: reco-venv
channels:
  - conda-forge
dependencies:
  - python=3.9

  # ML / Compiled libraries
  - flask
  - lightfm
  - numpy
  - scipy
  - scikit-learn
  - pandas
  - python-dotenv

  # Pip bridge
  - pip
  - pip:
      - fastapi
      - uvicorn
      - httpx
      - apscheduler
      - python-dotenv
